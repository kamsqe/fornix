# CLAUDE.md

Project context for AI assistants. Auto-generated by Fornix.

## Project Overview

- **Name:** test-flagdriven
- **Framework:** Astro
- **Render mode:** static
- **Deploy target:** cloudflare
- **CSS engine:** tailwind
- **Package manager:** pnpm

## Installed Blocks

- `hero-gradient` (variant: default)
- `features-grid` (variant: default)
- `footer-minimal` (variant: default)

## Styling

- **Palette:** midnight
- Colors are defined as CSS custom properties in `src/styles/palettes/_current.css`
- Use `var(--color-primary)`, `var(--color-secondary)`, etc. for theming

## Key Commands

- `pnpm dev` — start development server
- `pnpm build` — build for production
- `npx create-fornix add <block>` — add a new block
- `npx create-fornix remove <block>` — remove a block
- `npx create-fornix status` — show project configuration

## File Structure

```
src/
├── components/sections/  ← block components
├── content/              ← JSON/MD content (from blocks)
├── layouts/              ← page layouts
├── pages/                ← Astro pages (routes)
├── styles/               ← CSS and palette files
```
